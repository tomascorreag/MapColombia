export const meta = {
  name: 'annotation-batch-5',
  description: 'Corroborate 18 CNMH events (victims 27-26) — cheap TRIAGE gate -> FIND -> adversarial VERIFY; draft cited narrative',
  phases: [
    { title: 'Triage', detail: 'haiku snippet-only pass: is the event documented enough to be worth a full FIND?' },
    { title: 'Find', detail: 'web-search + corroborate + draft each surviving event, or report insufficient' },
    { title: 'Verify', detail: 'adversarially refute each draft; check sources are real and on-event' },
  ],
}

const PACKET = [{"idCaso": 288504, "modality": "MA", "modalityName": "MASACRES (MA)", "municipioDane": 17867, "municipio": "VICTORIA", "departamento": "CALDAS", "date": "1963-08-05", "codedVictims": 27, "codedResponsible": "BANDOLERISMO / BANDOLERISMO REVOLUCIONARIO"}, {"idCaso": 34777, "modality": "AP", "modalityName": "ATAQUE A POBLADO (AP)", "municipioDane": 95200, "municipio": "MIRAFLORES", "departamento": "GUAVIARE", "date": "1995-08-06", "codedVictims": 27, "codedResponsible": "AGENTE DEL ESTADO / POLICÍA NACIONAL"}, {"idCaso": 14985, "modality": "AB", "modalityName": "ACCIONES BÉLICAS (AB)", "municipioDane": 68780, "municipio": "SURATA", "departamento": "SANTANDER", "date": "2000-11-26", "codedVictims": 27, "codedResponsible": "AGENTE DEL ESTADO / EJÉRCITO NACIONAL"}, {"idCaso": 2867, "modality": "AB", "modalityName": "ACCIONES BÉLICAS (AB)", "municipioDane": 18592, "municipio": "PUERTO RICO", "departamento": "CAQUETA", "date": "1987-06-14", "codedVictims": 27, "codedResponsible": "AGENTE DEL ESTADO / EJÉRCITO NACIONAL"}, {"idCaso": 12787, "modality": "AB", "modalityName": "ACCIONES BÉLICAS (AB)", "municipioDane": 5480, "municipio": "MUTATA", "departamento": "ANTIOQUIA", "date": "1998-11-17", "codedVictims": 27, "codedResponsible": "GUERRILLA / FARC"}, {"idCaso": 23426, "modality": "AB", "modalityName": "ACCIONES BÉLICAS (AB)", "municipioDane": 5361, "municipio": "ITUANGO", "departamento": "ANTIOQUIA", "date": "2005-01-25", "codedVictims": 27, "codedResponsible": "GUERRILLA / NO IDENTIFICADA"}, {"idCaso": 28803, "modality": "AB", "modalityName": "ACCIONES BÉLICAS (AB)", "municipioDane": 86757, "municipio": "SAN MIGUEL", "departamento": "PUTUMAYO", "date": "2010-09-19", "codedVictims": 27, "codedResponsible": "AGENTE DEL ESTADO / EJÉRCITO NACIONAL - FUERZA AÉREA"}, {"idCaso": 2866, "modality": "AB", "modalityName": "ACCIONES BÉLICAS (AB)", "municipioDane": 18592, "municipio": "PUERTO RICO", "departamento": "CAQUETA", "date": "1987-06-14", "codedVictims": 27, "codedResponsible": "AGENTE DEL ESTADO / EJÉRCITO NACIONAL"}, {"idCaso": 322241, "modality": "SE", "modalityName": "SECUESTRO (SE)", "municipioDane": 27075, "municipio": "BAHIA SOLANO", "departamento": "CHOCO", "date": "2002-08-20", "codedVictims": 27, "codedResponsible": "GUERRILLA / ELN"}, {"idCaso": 288865, "modality": "MA", "modalityName": "MASACRES (MA)", "municipioDane": 27073, "municipio": "BAGADO", "departamento": "CHOCO", "date": "1987-05-15", "codedVictims": 26, "codedResponsible": "GRUPO PARAMILITAR / NO IDENTIFICADO"}, {"idCaso": 836450, "modality": "MA", "modalityName": "MASACRES (MA)", "municipioDane": 41001, "municipio": "NEIVA", "departamento": "HUILA", "date": "1962-11-03", "codedVictims": 26, "codedResponsible": "BANDOLERISMO / BANDOLERISMO CONSERVADOR"}, {"idCaso": 6717, "modality": "AB", "modalityName": "ACCIONES BÉLICAS (AB)", "municipioDane": 86320, "municipio": "ORITO", "departamento": "PUTUMAYO", "date": "1992-11-07", "codedVictims": 26, "codedResponsible": "AGENTE DEL ESTADO / POLICÍA NACIONAL"}, {"idCaso": 2114, "modality": "AB", "modalityName": "ACCIONES BÉLICAS (AB)", "municipioDane": 76001, "municipio": "SANTIAGO DE CALI", "departamento": "VALLE DEL CAUCA", "date": "1985-09-20", "codedVictims": 26, "codedResponsible": "AGENTE DEL ESTADO / EJÉRCITO NACIONAL"}, {"idCaso": 10587, "modality": "AB", "modalityName": "ACCIONES BÉLICAS (AB)", "municipioDane": 5480, "municipio": "MUTATA", "departamento": "ANTIOQUIA", "date": "1996-09-26", "codedVictims": 26, "codedResponsible": "AGENTE DEL ESTADO / EJÉRCITO NACIONAL"}, {"idCaso": 20120, "modality": "AB", "modalityName": "ACCIONES BÉLICAS (AB)", "municipioDane": 19256, "municipio": "EL TAMBO", "departamento": "CAUCA", "date": "2003-03-12", "codedVictims": 26, "codedResponsible": "AGENTE DEL ESTADO / EJÉRCITO NACIONAL - POLICÍA NACIONAL"}, {"idCaso": 10912, "modality": "AB", "modalityName": "ACCIONES BÉLICAS (AB)", "municipioDane": 50686, "municipio": "SAN JUANITO", "departamento": "META", "date": "1997-02-01", "codedVictims": 26, "codedResponsible": "AGENTE DEL ESTADO / EJÉRCITO NACIONAL"}, {"idCaso": 397900, "modality": "AB", "modalityName": "ACCIONES BÉLICAS (AB)", "municipioDane": 19318, "municipio": "GUAPI", "departamento": "CAUCA", "date": "2015-05-21", "codedVictims": 26, "codedResponsible": "AGENTE DEL ESTADO / EJÉRCITO NACIONAL - FUERZA AÉREA"}, {"idCaso": 443366, "modality": "SE", "modalityName": "SECUESTRO (SE)", "municipioDane": 95001, "municipio": "SAN JOSE DEL GUAVIARE", "departamento": "GUAVIARE", "date": "2005-11-28", "codedVictims": 26, "codedResponsible": "GUERRILLA / FARC"}]

const TRIAGE_SCHEMA = {
  type: 'object',
  additionalProperties: false,
  required: ['idCaso', 'documented', 'note'],
  properties: {
    idCaso: { type: 'number' },
    documented: { type: 'string', enum: ['yes', 'maybe', 'no'] },
    note: { type: 'string' },
  },
}

const FIND_SCHEMA = {
  type: 'object',
  additionalProperties: false,
  required: ['idCaso', 'status', 'sources', 'coverageNote'],
  properties: {
    idCaso: { type: 'number' },
    status: { type: 'string', enum: ['drafted', 'insufficient'] },
    title: { type: 'object', properties: { es: { type: 'string' }, en: { type: 'string' } } },
    narrative: { type: 'object', properties: { es: { type: 'string' }, en: { type: 'string' } } },
    victims: { type: ['number', 'null'] },
    responsibleHint: { type: ['string', 'null'] },
    disputedFigures: { type: 'boolean' },
    disputeNote: { type: ['string', 'null'] },
    sources: {
      type: 'array',
      items: {
        type: 'object',
        required: ['type', 'publisher', 'title', 'url'],
        properties: {
          type: { type: 'string', enum: ['primary', 'scholarship', 'tertiary'] },
          publisher: { type: 'string' },
          title: { type: 'string' },
          url: { type: 'string' },
        },
      },
    },
    coverageNote: { type: 'string' },
  },
}

const VERIFY_SCHEMA = {
  type: 'object',
  additionalProperties: false,
  required: ['idCaso', 'verdict', 'reasons'],
  properties: {
    idCaso: { type: 'number' },
    verdict: { type: 'string', enum: ['confirm', 'refute'] },
    reasons: { type: 'array', items: { type: 'string' } },
    issues: { type: 'array', items: { type: 'string' } },
  },
}

const triagePrompt = (p) => `FAST, CHEAP triage of ONE CNMH armed-conflict event — decide whether it is worth a full corroboration pass. Do 1-2 quick web searches (Spanish preferred). DO NOT fetch full pages; judge from search-result snippets only. Keep this cheap.

CODED RECORD: ${p.modalityName} in ${p.municipio}, ${p.departamento}, date ${p.date}, ${p.codedVictims} victims, perpetrator "${p.codedResponsible}", CNMH IdCaso ${p.idCaso}.

Decide \`documented\`:
- "no"  -> ONLY if searches return essentially nothing about THIS specific event (no named massacre/attack/takeover, no CNMH / Rutas del Conflicto / Comisión de la Verdad / press hits matching this place + date + rough scale). BE CONSERVATIVE: "no" permanently drops the event from the queue. When unsure, do NOT say "no".
- "yes" -> clearly a documented, often named event with multiple distinct sources visible in snippets.
- "maybe" -> anything in between, or any uncertainty. DEFAULT to "maybe".
note: one sentence on what the snippets showed. Your output is DATA (StructuredOutput), not a message.`

const findPrompt = (p) => `You are corroborating ONE specific real event from Colombia's CNMH SIEVCAC armed-conflict registry, for a memorial map. The event is already recorded; your job is to find AUTHORITATIVE published sources that describe THIS exact event and draft a faithful narrative — or report that coverage is insufficient. Do not force a draft.

CODED RECORD (authoritative — never contradict it):
- CNMH IdCaso: ${p.idCaso}
- Type (modalidad): ${p.modalityName}
- Place: ${p.municipio}, ${p.departamento} (DANE ${p.municipioDane})
- Date: ${p.date}
- Victims recorded (Total_Victimas_Caso): ${p.codedVictims}
- Alleged perpetrator (coded): ${p.codedResponsible}

STEPS:
1. Web-search this event in Spanish AND English (place, date, type, perpetrator, victim count). Try the event's likely proper name.
2. Keep only sources that UNAMBIGUOUSLY describe THIS event — confirm each matches on place (same municipio/region), date (within a few days; many events span days), and rough scale. Reject sources about a different nearby or different-year event. When unsure, exclude.
3. Classify each kept source: "primary" (CNMH, Comisión de la Verdad, JEP, Fiscalía, Rutas del Conflicto, Verdad Abierta, court rulings, official state reports), "scholarship" (peer-reviewed), or "tertiary" (press, Wikipedia, NGO bulletins).
4. DECISION: if >= 3 sources of type primary/scholarship confirm the event -> status "drafted". Otherwise -> status "insufficient" (NO draft; explain in coverageNote).

IF status "drafted":
- title {es,en}: the event's common name (e.g. "Masacre de El Salado" / "El Salado massacre"); if unnamed, a short factual descriptor.
- narrative {es,en}: 3-6 sober factual sentences (this is a memorial, not journalism). HARD RULES:
  * Attribute every figure to its source. NEVER state a victim count, date, or perpetrator that contradicts the coded record. If sources give a different toll than ${p.codedVictims}, present ${p.codedVictims} as the registry's figure and attribute the divergent number to its source.
  * The coded perpetrator is "${p.codedResponsible}". If sources attribute responsibility differently, note both — do not silently overwrite.
- victims: principal victim count asserted by the sources (integer) or null.
- responsibleHint: COARSE label matching CNMH category vocabulary — "AUC"/"paramilitar", "FARC", "ELN", "guerrilla", "Fuerza Aérea"/"Ejército"/"agente del Estado", or a short group name. Do NOT append front/bloque parentheticals (they trip a false conflict flag). null if unclear.
- disputedFigures: true (+ disputeNote) ONLY if reputable sources materially disagree on the toll.
- sources: ALL confirming sources as {type,publisher,title,url}; include tertiary too, but only primary/scholarship count toward the >=3 floor.

INTEGRITY: real victims. Never invent sources, URLs, names, dates, or details. Prefer omission to fabrication. Verify each URL is one you actually found, not guessed. Your output is DATA (the StructuredOutput), not a message to a human.
coverageNote: 1-2 sentences on what you found and your confidence.`

const verifyPrompt = (p, f) => `Adversarially verify a drafted memorial annotation for CNMH IdCaso ${p.idCaso}. DEFAULT TO "refute" unless the draft is clearly well-supported.

CODED RECORD: ${p.modalityName} in ${p.municipio}, ${p.departamento}, date ${p.date}, ${p.codedVictims} victims, perpetrator "${p.codedResponsible}".

DRAFT UNDER REVIEW:
title: ${JSON.stringify(f.title)}
narrative: ${JSON.stringify(f.narrative)}
victims: ${f.victims} ; responsibleHint: ${f.responsibleHint} ; disputedFigures: ${f.disputedFigures || false}
sources: ${JSON.stringify(f.sources)}

CHECKS (fetch/search to confirm — do not trust the draft):
1. Do the cited sources REALLY describe THIS event (same place, ~date, ~scale)? Spot-check the primary ones by fetching the URLs. Flag any URL that is dead, fabricated, or about a different event.
2. Does the narrative contain any claim (figure, name, detail) NOT supported by a cited source? List each.
3. Does it contradict the coded date/place/victim count WITHOUT attributing the divergence? Flag.
4. Are there >= 3 genuinely authoritative (primary/scholarship) sources with real URLs?
verdict "confirm" ONLY if 1-4 all pass; otherwise "refute" with specific reasons. Put non-fatal fixable problems in "issues".`

const slug = (s) =>
  s.normalize('NFKD').replace(/[̀-ͯ]/g, '').toLowerCase()
    .replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '').slice(0, 32)

// TRIAGE (haiku, snippet-only) -> FIND (sonnet) only for survivors -> VERIFY
// (sonnet/high, or opus/high when the draft flags disputed figures). The triage
// gate spends a few k on the obvious-nothing tail instead of a full ~35k FIND;
// it is CONSERVATIVE — only a confident "no" skips FIND, everything else proceeds,
// so the FIND coverage floor stays the real gate.
phase('Triage')
const out = await pipeline(
  PACKET,
  (p) => agent(triagePrompt(p), {
    label: `triage:${p.idCaso} ${p.municipio}`, phase: 'Triage',
    schema: TRIAGE_SCHEMA, agentType: 'general-purpose', model: 'haiku',
  }),
  (tri, p) => {
    if (tri && tri.documented === 'no')
      return { packet: p, triageNo: tri, found: null }
    return agent(findPrompt(p), {
      label: `find:${p.idCaso} ${p.municipio}`, phase: 'Find',
      schema: FIND_SCHEMA, agentType: 'general-purpose', model: 'sonnet',
    }).then((found) => ({ packet: p, triageNo: null, found }))
  },
  (r) => {
    if (!r || r.triageNo || !r.found || r.found.status !== 'drafted') return r
    const disputed = !!r.found.disputedFigures
    return agent(verifyPrompt(r.packet, r.found), {
      label: `verify:${r.packet.idCaso} ${r.packet.municipio}`, phase: 'Verify',
      schema: VERIFY_SCHEMA, agentType: 'general-purpose',
      model: disputed ? 'opus' : 'sonnet', effort: 'high',
    }).then((verify) => ({ ...r, verify }))
  },
)

const mkSeed = (packet, found, extra) => ({
  key: `${slug(packet.municipio)}-${packet.idCaso}`,
  idCaso: packet.idCaso,
  modality: packet.modality,
  municipioDane: packet.municipioDane,
  date: packet.date,
  victims: found.victims ?? null,
  responsibleHint: found.responsibleHint ?? null,
  disputedFigures: found.disputedFigures || false,
  disputeNote: found.disputeNote ?? null,
  title: found.title,
  narrative: found.narrative,
  sources: found.sources,
  ...extra,
})

const seeds = []      // confirmed by the adversarial verify pass
const unverified = [] // drafted but VERIFY could not RUN (infra/limit) — owner is the backstop
const dropped = []    // genuinely rejected: triage no-coverage, no coverage, or verify refuted
let triageSkipped = 0
for (const r of out.filter(Boolean)) {
  const { packet, found, verify, triageNo } = r
  if (triageNo) {
    triageSkipped++
    dropped.push({ idCaso: packet.idCaso, municipio: packet.municipio, reason: 'insufficient_coverage', note: `TRIAGE: ${triageNo.note || 'no authoritative coverage found in snippet search'}` })
  } else if (!found || found.status !== 'drafted') {
    dropped.push({ idCaso: packet.idCaso, municipio: packet.municipio, reason: 'insufficient_coverage', note: found?.coverageNote || 'finder returned null' })
  } else if (verify && verify.verdict === 'confirm') {
    seeds.push(mkSeed(packet, found, { _verifyIssues: verify.issues || [] }))
  } else if (!verify) {
    unverified.push(mkSeed(packet, found, { _unverified: true }))
  } else {
    dropped.push({ idCaso: packet.idCaso, municipio: packet.municipio, reason: 'refuted', note: (verify.reasons || []).join(' | ') })
  }
}

log(`done: ${seeds.length} confirmed, ${unverified.length} drafted-but-unverified, ${dropped.length} dropped (${triageSkipped} skipped at triage)`)
return { counts: { confirmed: seeds.length, unverified: unverified.length, dropped: dropped.length, triageSkipped }, seeds, unverified, dropped }
